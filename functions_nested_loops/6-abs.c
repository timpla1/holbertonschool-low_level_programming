#include "main.h"

/**
 * _abs - absulute ?
 *
 * Return: Always 0.
 */

int _abs(int n)
{	
	if (n > 0)
	{
	_putchar(n);
	}
	if (n < 0)
	{
	_putchar(-n);
	}
	if (n == 0)
	{
	puthcar('0');
	}
	return (0);
}
